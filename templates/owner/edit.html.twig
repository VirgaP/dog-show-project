{% extends 'base.html.twig' %}

{% block title %}Edit Owner{% endblock %}

{% block body %}
    <div class="container-fluid symfony-form app-form">

    <h1>Edit Member Information</h1>

    {{ include('owner/_form.html.twig', {'button_label': 'Update'}) }}

        {% if is_granted ('ROLE_ADMIN') %}
    <a href="{{ path('member_index') }}">Go members list</a>

    {{ include('owner/_delete_form.html.twig') }}
        {% endif %}
    </div>
{% endblock %}