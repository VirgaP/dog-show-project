{% extends 'base.html.twig' %}

{% block title %}ShowClass{% endblock %}

{% block body %}
    <h1>ShowClass</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ show_class.id }}</td>
            </tr>
            <tr>
                <th>ClassTitle</th>
                <td>{{ show_class.classTitle }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('show_class_index') }}">back to list</a>

    <a href="{{ path('show_class_edit', {'id': show_class.id}) }}">edit</a>

    {{ include('show_class/_delete_form.html.twig') }}
{% endblock %}