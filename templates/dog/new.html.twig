{% extends 'base.html.twig' %}

{% block title %}New Dog{% endblock %}

{% block body %}
<div class="container-fluid symfony-form app-form">

<h1>Register your pet</h1>
    <small>You only need to register your pet once.</small>

    {{ include('dog/_form.html.twig') }}
    {#{{ include('dog/_form.html.twig', {'button_label': 'Save'}) }}#}

    <a href="{{ path('dog_index') }}">back to list</a>
</div>
{% endblock %}
