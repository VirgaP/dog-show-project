{% extends 'base.html.twig' %}
    <div class="container-fluid symfony-form app-form">

{% block title %}Edit homepage{% endblock %}
{% block navbar %} {% endblock %}
{% block body %}
    <div class="container-fluid">
        <h1>Edit the home page information</h1>

        {{ form_start(form) }}
        {{ form_widget(form) }}
        <button class="btn">{{ button_label|default('Save') }}</button>
        {{ form_end(form) }}

        <a href="{{ path('home') }}">Back to homepage</a>
    </div>
    </div>
{% endblock %}